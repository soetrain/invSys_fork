# Slice 4be Production validation and Action Path integration

Last verified: 2026-09-24 UTC. **Acceptance incomplete.** Architecture v4.11 D18
governs the same six Production draft controls and their explicit local-command
terminal mappings. No saved or Domain-applied design conclusion follows from
local STAGED/VALIDATED observations with empty source references.

## Approved Event Detail candidate: visible continuation

The 2026-09-24 run on `validation-approved-detail-scroll` reaches **379 PASS /
one capture-harness exception**, with 11 preceding checks unreached. It retains
all reached prior GREEN checks and supplies nine individually reviewed images:
both designers, both published event details, positive/rejected Process and
Recipe conclusions, and the Process missing-Domain-evidence conclusion.
The Recipe missing-Domain-evidence capture fails because no uncovered owned
form-caption point is available; it is not Windows error5 and its cause is unproven.
Report: `slice4be-production-designer/acd46fd3cd8844d9b1c0e97044a27c30/green.json`;
controller `b8c171108d4a4142911de3b55ad558bb` under `reports/runtime`.
UTC **16:47:18--16:55:40**. Excel closes without assistance after waiting;
settings and packages are preserved. This partial attempt is not GREEN.

The existing blank-form calibration subsequently captures all three visibility
cases. A read-only capture-failure diagnostic now records window identities,
caller-DPI bounds and enabled/minimized flags, without captions or workbook data.
It changes no activation rule, input target or capture acceptance. Parsing and
the repeated three-case calibration pass; the latter report is
`capture-foreground-calibration/42f1e6bf35d74c90ab181dc6c290c02e` under runtime.
The failed attempt is retained before a single capture-enabled retry.

That retry records **340 PASS / one harness exception**, with 50 preceding
checks unreached. Controller `480ea0e230e04854b1ca96cf22865635`, report
`slice4be-production-designer/9fcdc6180b124840b1418dacc861495d/green.json`, UTC
**16:57:10--17:01:08**. Excel rejects the shared RecordingExpectationForTest
boundary with **0x800AC472** after the Process Clear expectation-editor check.
The original log does not distinguish the Evaluate click from the following
status/value reads; do not infer which failed or replay a possibly executed command.
Excel closes without assistance and settings/packages are preserved. Further
broad Production retries stop pending focused boundary diagnosis. Failure
diagnostics now record fixed expectation form/control/action names without field
values and can capture only an already-owned allowlisted foreground form.
No retry policy, input target, runtime contract or acceptance result is changed.

The focused coordination audit finds that the Production controller omitted the
existing bounded pre-dispatch Excel readiness observation used by the passing
Viewer and owner controllers. It now enables that same 40-read limit; commands
still dispatch once and are never replayed. The standalone actual-dispatcher
calibration initializes its missing fixture context flags and passes **44/44**,
including busy/unavailable bounds, redaction and failure-without-replay cases.
Report: `ready-before-dispatch/ccff00f4999a4b23853eeab4b3639478/results.json`.
This is a harness coordination correction, not proof of the earlier exception's
root cause or a product behavior change. A subsequent capture-enabled run uses
controller `67960ea6bc4944cdbf47c18e7a34a133`; its result is recorded only after
terminal closure and evidence review.

That run completes **390/390 GREEN**, retaining every previous identity, with
five instrumented compiles and all ten images individually reviewed: Process
and Recipe designers, both published Details, and both positive, rejected and
missing-Domain-evidence diagnostic conclusions. Normal delayed Excel closure,
settings restoration and five package hashes pass. All 273 tooling hashes were
verified before the later standalone diagnostic-fixture update. UTC
**17:25:28--17:34:03**, with zero Application events 1000/1001/1002. Report:
`slice4be-production-designer/54878f9a2b874b10878c6d4b1f6a0491/green.json`.
Private receipt: `reports/runtime/approved-detail-scroll-production-ready-verification.json`.
This establishes the scoped visible Production gate on the approved Detail
candidate; it does not prove the cause of either earlier harness failure.

The final offline failure-diagnostic calibration passes **83/83**, retaining
the existing 78 identities and adding five expectation-handler checks for exact
fixed identity, entered-field exclusion, original error retention,
no capture request and one dispatch without replay. This fixture-only extension
occurs after the packaged gate and changes no runtime/package. Full Production
control coverage and Slice 4be acceptance remain open.

## Valid Recipe validation

The fixture enables Designs through the actual Admin Save Value handler, then
uses actual Production New, output Add, Save and Release handlers for a fresh
Process identity. It verifies DRAFT then RELEASED from the owning query, adds
that released Process through the real Recipe handler and reads its output and
RELEASED status through the Designs primitive bridge. No validator or record is
replaced. Preservation snapshots follow intentional fixture setup.

Pre-observation palette candidate RED: **67 PASS/177 expected FAIL**,
04:24:11--04:26:27 UTC. All ten new Recipe checks are reached: real release,
validation, unchanged draft/saved bytes pass; six missing-observation checks fail.
The other failures retain the established observation/context/catalog RED.
Report: `reports/runtime/slice4be-production-designer/e0bf0cbb830a4f7f96a65d130259aa47`;
controller `production-designer-controller/209d9cb4638e4c28b2bcdda0ce09137c`.

Current caption candidate GREEN: **244/244**, 04:26:45--04:29:18 UTC, retaining
every RED and prior 234 GREEN identity. Five instrumented compiles pass in both
attempts. Settings/packages are preserved and Excel closes without assistance;
the GREEN controller waits for eventual process exit. No runtime edit or rebuild
is needed. Report: `slice4be-production-designer/22db36e3d2ea4dd48a3a0d8ba6d2beb8`;
controller `production-designer-controller/68dffc9a114b4e25b73858ccc220b28c`.
Receipt: `reports/runtime/production-recipe-focused-verification.json`.

## Recording, Viewer and diagnostics

Two adapter calibration attempts are retained separately from behavioral RED.
The first stops at 278 PASS/one fixture exception because the rebound designer
has not loaded the released-Process list; the adapter now invokes the actual
Refresh handler before recording starts. Controller: `148bb335cad045cfa68189174d11f526`.
The second reaches 338 PASS/six FAIL before an invalid terminal ListIndex stops
the adapter after the real editor reports its missing VALIDATED choice. Its
VBA dialog is ended explicitly; subsequent cleanup is assisted, not accepted
unassisted closure. Controller: `3fb9a7ad56434b7882b6d4bf7f94553a`.
Both preserve candidate packages and restore settings. Neither is the completed
diagnostic RED gate. The shared draft helper now optionally cancels on a missing
choice before trying to add/select a nonexistent terminal step.

The second attempt also exposes a test assumption about published detail-line
position. Existing D18 activity grouping preserves exact records/timestamps and
orders groups; it does not require filesystem-fed Lines to follow journal order.
The corrected check compares every complete original record by exact RecordId,
while the separate 18-record stopped journal check retains its exact action order.
The new normative paragraph explicitly inherits that existing wire contract.

The extended gate uses actual Viewer Start/Stop, eight actual Production draft
actions (six positive outcomes and both rejected validations), ordinary Admin
publication, Viewer list/selection and packaged expectation/Evaluate handlers.
It protects original record identities/order, exact contributing activity pairs,
immutable activity/journal bytes and saved authority. Explicit Domain-evidence
expectations must remain incomplete for these empty-reference observations.

The predecessor expectation editor omits VALIDATED and its explicit evaluator
owner map omits these six controls. Normative specification, Plan022 and controls
name STAGED for the exact four NEW/CLEAR controls and VALIDATED for the two
VALIDATE controls, inheriting the existing D18 Settings staging distinction.
Completed behavioral RED: **370 PASS/eight expected FAIL**, 378 checks, retaining
all 244 prior GREEN identities. Four NEW/CLEAR completion checks fail and four
VALIDATED expectation choices fail (two CommandCompleted and two SourceEventsApplied).
All publication, recording, immutable-byte, rejected-validation and request-only
checks pass. Five instrumented compiles pass; no harness exception occurs.
04:48:52--04:56:38 UTC, controller `8bd92da5bfc34cbda9851e639258cded`, report
`slice4be-production-designer/52bc0288178842e6a01dd6dbf14a7b57/red.json`.
Excel exits without assistance after the controller waits; settings/packages are
preserved. Receipt: `reports/runtime/production-diagnostics-red-verification.json`.

Only after this RED, modExpectationDraft adds the catalog-filtered VALIDATED
choice and modEvaluationMatches adds four lines mapping these exact six controls.
The separately built `deploy/validation-production-diagnostics` candidate passes
all five package builds, explicit compiles and Operations cold start. Compiled
comparison retains 244 components and changes only modExpectationDraft and
modEvaluationMatches. Static: 251 components, 6050 procedures, 133025 lines;
9 literal/45 unresolved dynamic calls and 193 duplicate groups unchanged.
All 28 oversized-module ratchets are unchanged; all three schemas validate.
274 PowerShell files parse. Existing source layout gates pass 7/7 and 8/8.
Receipts: `production-diagnostics-component-verification.json` and
`production-diagnostics-static-verification.json` under `reports/runtime`.

The first candidate GREEN attempt reaches **315 PASS/one harness exception**,
04:58:35--05:02:04 UTC, before desktop cursor positioning fails during Event Detail
capture. It is not GREEN. Controller `ec9b2cf4f1f34b21bef8a913630c9042`; report
`slice4be-production-designer/6fc9621d823b4290a1c33840e9a726ad`. Both captured
Production designers are reviewed: readable, unobscured, with successful local
Process/Recipe validation visible. No Detail or diagnostic capture is established.
Excel closes unassisted and settings/package preservation passes.

At 05:03:34 UTC, a standard, non-elevated shell on WinSta0/Default reproduces
GetCursorPos error 5. At 05:04:25 UTC, WTS reports an active RDP session and
UOI_IO=False for the thread desktop. At 05:04:54 UTC, both the thread desktop and
opened input-desktop handle report UOI_IO=False. These are observed preconditions,
not proof that elevation or a particular RDP transition caused the failure.
At 05:06:55 UTC, the same non-elevated test environment successfully opens WinSta0
requesting WINSTA_READATTRIBUTES|WINSTA_WRITEATTRIBUTES (0x12), without changing
permissions. This demonstrates that those access rights can be obtained while
the observed input-desktop condition remains the stronger immediate lead.
No desktop permissions, UAC settings or session configuration is changed.
Microsoft's [SetCursorPos contract](https://learn.microsoft.com/en-us/windows/win32/api/winuser/nf-winuser-setcursorpos)
requires the current input desktop and window-station write access; its
[UI automation guidance](https://learn.microsoft.com/en-us/troubleshoot/power-platform/power-automate/desktop-flows/ui-automation/uipi-issues)
identifies locked desktops and minimized RDP clients among possible causes.
Further visible retries require changed access; a separate capture-disabled
behavioral run does not substitute for the remaining visible evidence.

The capture-disabled candidate run passes **390/390**, retaining all 378 RED
identities and all 244 prior GREEN identities. The twelve additional assertions
become reachable when VALIDATED is offered by the editor. All six positive owner
mappings, rejected/request-only negative cases and both empty-source Domain
expectations pass through actual packaged editor/Evaluate actions. Original
activity/journal/publication and saved authority remain unchanged.
Controller: `434daf393a934932acddba2d84af2304`; report:
`slice4be-production-designer/5e01a29b8a7e42ed8b41322696ae639c/green.json`.
Run interval: 05:03:35--05:13:30 UTC. Excel exits without assistance after the
controller waits. Settings, five candidate packages, 215 test scripts and three
unrelated edits are preserved. No Excel Application 1000/1001/1002 failure events
are found through 05:13:47 UTC. Receipt:
`reports/runtime/production-diagnostics-focused-verification.json`.

## Remaining acceptance

Only the two Production designer images are accepted from this candidate.
Event Detail and diagnostic images remain required after desktop input recovers.
The latest read-only probe at 05:12:20 UTC still returns cursor error 5 and
UOI_IO=False. Current-candidate owner-command and full-chain/live-role regressions
are pending because their visible evidence/dialog input needs that desktop.
Prepared private wrappers use `run-production-diagnostics-` prefixes and preserve
settings, prior reports and package hashes. No accepted deployment is changed.
Earlier 460 owner, 202 Settings, 67 reusable observations and 32/48/15 chain/live/
Create Warehouse results retain their earlier candidate scope. Broader coverage,
guide comparison and pending architecture decisions remain open. **Not full
six-control, Slice 4be or Release 1 acceptance.**
