# Plan 022 Slice 4be Boxing published Event Viewer evidence

The subsequent [activity-label correction](plan022_slice4be_event_detail_labels_results.md)
records the focused RED/GREEN for the picker finding below and its remaining gates.

## Scope and protecting route

This supplementary 4be.1/4be.3 test applies Architecture v4.11 D18's existing
Boxing source-reference, complete publication and read-only Event Detail rules.
It changes no runtime contract or package. The earlier
[Boxing implementation RED/GREEN](plan022_slice4be_boxing_activity_results.md)
remains the protecting implementation evidence; a first-pass supplementary
test is not a retrospective RED.

The four actual Make/Unbox actions supply eight immutable observations and two
independently verified Inventory events, each with its package/component lines.
After the existing Shipping/Boxing route finishes, the ordinary Admin snapshot
command publishes them. The actual Viewer search/list selection and Event Detail
line handler must expose every exact identity, recorded outcome and data effect,
distinguishing User activity from Business event. No synthetic projection replaces
these records. Read preservation covers all generated warehouse/training bytes
and the unrelated workbook; existing counters check no implicit publication or
Shipping authority supplement read.

All test facades are installed before the existing five instrumented package
compiles and are never saved into the candidate. Reports contain fixed checks
and booleans; visible evidence uses only disposable generated fixture windows.

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tests/tooling/Test-Slice4beConfigCommands.ps1 `
  -RepoRoot . -DeployRoot deploy/validation-boxing-activity-visible -Phase GREEN `
  -CaptureEvidence -CheckActivityEvidence -CheckActivityFoundation `
  -CheckShippingActivity -CheckBoxingActivity
```

## Verification status

The unchanged candidate completes **1689 PASS / 7 known D8-A FAIL** across
1696 checks. All **112 new published-read checks** and **680 Boxing checks** pass.
Every preceding 1584 check identity, all 1577 GREENs and all 18 independent
owner-return records are retained. There are no duplicate checks or harness
exceptions. The seven recorded D8-A checks remain outside this change; that
proposed architecture decision is unapproved. The process exits 1 for those
seven failures; it is not an entirely passing Release 1 gate.

Exact report:
`reports/runtime/slice4be-shipping-activity/ec2fe9d504284b1fa44b87675ff4e746/boxing-activity-shipping-recording-green.json`.
The ignored `boxing-published-read-` log, exit, verification, native and capture-review
records under `reports/runtime/` preserve the independent checks. All five
instrumented projects compile. The six new Event Detail captures are inspected;
the existing sixteen captures are also retained, without claiming a new review
of those unchanged-role views. Excel closes, and no Excel Event1000 appears in
the verified `2026-09-15T03:06:37Z` to `03:23:31Z` window. This does not establish
repair of earlier native failures.

Fresh static evidence in `reports/runtime/boxing-published-read-static/` retains
all 232 component sizes, 5880 procedures, 130081 lines, 191 duplicate groups and
9/45 dynamic calls. All 28 module growth limits, 325 historical package pins,
the five candidate hashes and both unrelated user-document hashes are preserved.
Four changed/new PowerShell scripts parse. No runtime source or XLAM changes;
the candidate's previously recorded compile/cold-start, layout, Settings,
Receiving, evaluation, native worksheet, live-role and full-chain results retain
their original scope and are not represented as rerun here.

## Visible finding and next protecting test

Both business-event captures show the two exact entity lines and the selected
line's independent quantity. The activity captures show correct event code,
outcome, owning operation, data effect and source classification. The test selects
and verifies both records in each activity group. Captures show the last line in
published order, which is not necessarily the outcome; publication line order is
not a claim of chronological sequence.

The activity line picker currently displays **Unavailable** twice because each
observation has no System_Key. Its inventory-only prompt is also confusing for
activity. No missing inventory identity should be invented. Next refine that
presentation under D18 semantic inheritance, synchronize the normative display
rule and controls, and establish actual-line-picker RED for fixed activity
caption/outcome labels before changing Operations. Preserve exact inventory keys,
repeated contributing lines, current-state limitations and cached read-only selection.

The current test covers the configured default detail display. Boxing-specific
visibility/detail policy changes, the activity-picker correction, combined cross-role recordings, remaining control
coverage, guide authoring/version/search/export/import, How-To/Diagnostic/Compare,
physical deployment and human Release 1 acceptance remain required. Automated form
captures are not human acceptance. Accepted deployment and operational workbooks
remain unchanged.
