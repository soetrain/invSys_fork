# deploy

Deployment packaging layout for `invSys`.

- `current/`: latest validated XLAM/workbook release package.
- `archive/`: historical release packages.

This directory was added to align with `invSys-Design-v4.2.md` repository structure.
